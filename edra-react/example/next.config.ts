import type { NextConfig } from "next"

const nextConfig: NextConfig = {
  // Keep transpilePackages for potential future use with the edra-react package
  transpilePackages: ["edra-react"],
}

export default nextConfig
