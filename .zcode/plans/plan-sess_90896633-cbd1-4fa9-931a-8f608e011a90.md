# Personal Knowledge Garden — Phased Implementation Plan

Built from `PRD.md` + `TRD.md`. Greenfield repo: all code goes under `code/`. Git is initialized inside `code/` and drives history commit-by-commit. Editor = **Tiptap**. Execution scope after approval = **Phase 0 scaffolding only**, then pause for review.

---

## Stack (from TRD §2, locked)

- **Hosting:** Cloudflare Workers (Next.js via `@opennextjs/cloudflare`)
- **Framework:** Next.js App Router + TypeScript + `src/` dir
- **Styling:** Tailwind CSS + `@tailwindcss/typography`
- **DB:** Supabase Postgres · **ORM:** Drizzle (`postgres.js` driver, pooled conn)
- **Auth:** Supabase Auth (`@supabase/ssr`, HTTP-only cookie, single admin)
- **Storage:** Supabase Storage (S3-compatible, `public-images` bucket)
- **Editor:** **Tiptap** (Phase 1)
- **Markdown render:** `react-markdown` + `rehype-pretty-code` (Shiki)
- **Caching:** Cloudflare Cache API keyed by `content_version`
- **CI/CD:** GitHub Actions → `wrangler deploy`

---

## Target Directory Structure (under `code/`)

```
code/
├── .github/workflows/ci.yml
├── .gitignore · .env.example · README.md · PLAN.md
├── package.json · tsconfig.json · next.config.ts
├── tailwind.config.ts · postcss.config.mjs
├── drizzle.config.ts
├── open-next.config.ts
├── wrangler.jsonc
├── drizzle/                       # generated migrations
└── src/
    ├── middleware.ts              # auth gate for /admin & non-GET /api
    ├── db/
    │   ├── schema.ts             # Drizzle schema = TRD §4 tables
    │   └── client.ts             # edge Drizzle client (pooled conn)
    ├── lib/
    │   ├── supabase/server.ts    # cookie-based server client
    │   ├── supabase/admin.ts     # service-role client (writes)
    │   ├── auth.ts               # getSession / requireAdmin
    │   ├── slug.ts · reading-time.ts
    │   ├── cache.ts              # content_version-keyed edge cache
    │   └── theme.ts              # dark-mode cookie helper
    ├── components/
    │   ├── ui/                   # Button, Input, Card primitives
    │   ├── layout/               # Header, Footer, Nav, ThemeToggle
    │   ├── editor/               # Tiptap MarkdownEditor (Phase 1)
    │   ├── tags/                 # TagInput, TagBadge, TagFilter
    │   └── markdown/             # Renderer + ToC (Phase 3)
    └── app/
        ├── layout.tsx · globals.css · page.tsx (Home, Phase 5)
        ├── blogs/{page,[slug]/page}.tsx        (Phase 3)
        ├── portfolio/{page,[slug]/page}.tsx    (Phase 4)
        ├── admin/{layout,page}.tsx + thoughts/ blogs/ projects/ tags/ analytics/ login/
        └── api/{thoughts,blogs,projects,tags,upload,analytics/track}/route.ts
```

---

## Cross-Cutting Conventions (all phases)

- **One commit per logical unit**, conventional-commit messages (`feat:`, `chore:`, `fix:`).
- **Env secrets never committed.** `.env.example` documents names; real values live in `wrangler` secrets / CI.
- **Drizzle migrations are the source of truth** — schema changes always ship with a generated `.sql` migration in `drizzle/`.
- **RLS is mandatory** on every table (TRD §4): `thoughts` = admin-only; `blogs`/`projects` = public read where `is_published`; `page_views` = insert-only.
- **Public routes cacheable** (`Cache-Control` + Cache API); `/admin/*` and non-GET `/api/*` = `no-store`.

---

# Phase 0 — Foundations (scaffolding, no features) ← EXECUTE NOW

**Goal:** Bootable Next.js app, typed DB layer, auth scaffolding, Cloudflare/OpenNext build pipeline, CI — committed to git. No product features yet.

**Exit criteria (TRD §7):** Hello-world app builds, lint+typecheck pass in CI, Drizzle schema compiles against Supabase schema, `requireAdmin` middleware compiles. (Live deploy + actual login require your Supabase/Cloudflare accounts + env — wired and ready, verified locally.)

**Steps & commits:**

1. **Repo init + plan doc**
   - `cd code && git init`, set default branch `main`.
   - Write `PLAN.md` (this document), `README.md` (short project intro + local-dev instructions), `.gitignore` (node_modules, `.next`, `.open-next`, `.wrangler`, `.dev.vars`, `.env*` except `.env.example`, etc.).
   - Commit: `chore: init repo, add plan and project docs`

2. **Scaffold Next.js**
   - `create-next-app@latest .` flags: `--typescript --tailwind --app --eslint --src-dir --no-import-alias` (then add `@/*` path alias manually in `tsconfig.json`).
   - Add path alias, verify `npm run dev` boots.
   - Commit: `chore: scaffold next.js app router + tailwind + ts`

3. **Install core dependencies**
   - Data: `drizzle-orm`, `postgres`, `drizzle-kit` (dev)
   - Auth/Supabase: `@supabase/ssr`, `@supabase/supabase-js`
   - Deploy: `@opennextjs/cloudflare`, `wrangler` (dev), `@cloudflare/workers-types` (dev)
   - Markdown/edit: `@tiptap/react @tiptap/starter-kit @tiptap/pm @tiptap/extension-link @tiptap/extension-placeholder`, `react-markdown`, `rehype-pretty-code`, `shiki`, `rehype-slug`, `reading-time`
   - Tailwind: `@tailwindcss/typography`
   - Misc: `clsx`, `tailwind-merge`, `zod`, `nanoid`/slugify lib
   - Commit: `chore: add runtime + dev dependencies`

4. **Drizzle schema + client (mirrors TRD §4)**
   - `src/db/schema.ts`: `tags`, `thoughts`, `blogs`, `projects`, `thought_tags`, `blog_tags`, `project_tags`, `images`, `page_views` — pg enums for `thought.status`, proper FKs + cascade, `gen_random_uuid()` defaults.
   - `drizzle.config.ts` (pooled `DATABASE_URL`, migrations → `drizzle/`).
   - `src/db/client.ts`: edge-safe `drizzle(postgres(...))` using pooled conn; flag the Workers-TCP requirement (pooled/transaction-mode conn string, `connect()`-compatible) as a deploy-verification item.
   - `npm run db:generate` → first migration `drizzle/0000_init.sql`. Commit: `feat(db): add drizzle schema + initial migration`
   - Note: applying the migration to live Supabase needs your `DATABASE_URL` (Phase 0 deliverable is code-ready; you run `db:push`/migrate once creds exist).

5. **Supabase client + auth scaffolding**
   - `src/lib/supabase/server.ts`: `@supabase/ssr` cookie client for RSC/route handlers.
   - `src/lib/supabase/admin.ts`: service-role client (only used server-side for writes/analytics insert).
   - `src/lib/auth.ts`: `getSession()`, `requireAdmin()` (throws/redirects to `/admin/login`).
   - `src/middleware.ts`: gate `/admin/*` + non-GET `/api/*` → require session; `no-store`. Stub compiles without live creds.
   - Minimal `src/app/admin/login/page.tsx` (email/password form posting to a route handler — real auth wired, verify once creds exist).
   - Commit: `feat(auth): supabase ssr clients + admin middleware + login stub`

6. **Cloudflare/OpenNext + build pipeline**
   - `open-next.config.ts`, `wrangler.jsonc` (compat flags, `assets` binding, vars/secrets placeholders: `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `DATABASE_URL`).
   - `next.config.ts`: enable OpenNext compatibility as needed.
   - npm scripts: `build`, `preview` (open-next build + `wrangler dev`), `db:generate`, `db:push`, `db:studio`.
   - `.env.example` documenting all required env vars.
   - Commit: `chore(deploy): add opennext + wrangler config, npm scripts`

7. **CI (GitHub Actions)**
   - `.github/workflows/ci.yml` on push/PR: `pnpm/npm ci` → `npm run lint` → `tsc --noEmit` → `npm run build`. (Deploy step added in Phase 7; secrets not present yet.)
   - Commit: `ci: add lint/typecheck/build workflow`

8. **Hello-world home + verify**
   - `src/app/page.tsx`: minimal landing card confirming stack wired (Supabase/Drizzle status read via admin client, rendered server-side — degrades gracefully without creds).
   - Local verification I'll run: `npm run lint`, `tsc --noEmit`, `npm run build`.
   - Commit: `feat: hello-world home page wired to supabase+drizzle`

**Phase 0 ends here — PAUSE for your review before building features.**

---

# Phase 1 — Thought Capture (writing core)

**Exit criteria:** Write, tag, revise private thoughts from any device as admin.

- `thoughts` migration + RLS policy (admin-only).
- `/api/thoughts` CRUD route handlers (zod-validated, `requireAdmin`).
- `/admin/thoughts` list + `[id]` edit pages.
- **Tiptap editor** (`components/editor/MarkdownEditor.tsx`): starter-kit + link + placeholder; **autosave** = debounced `PATCH` on content change; serialize to markdown.
- Tag input UI (autocomplete existing tags, create-on-the-fly) + `thought_tags` junction writes.
- Mobile: editor collapses toolbar, keeps core formatting.

---

# Phase 2 — Draft → Publish Workflow

**Exit criteria:** A thought can become a published (not-yet-public) blog or project doc.

- `blogs`, `projects` migrations + RLS (public read where `is_published`).
- `src/lib/slug.ts` (unique slug gen), `src/lib/reading-time.ts` (computed on save).
- "Publish as Blog" / "Publish as Project" actions from a thought → copy `content_md`, set `thought.status` + `published_ref`, create blog/project row.
- Admin edit views for published blogs/projects (edit-in-place per TRD §5.1 MVP).
- `blog_tags` / `project_tags` junctions + tag UI reused.

---

# Phase 3 — Public Blogs Section

**Exit criteria:** Published blogs public, fast, correctly cached.

- `/blogs` index: list published, tag filter.
- `/blogs/[slug]`: title, date, tags, reading time, `react-markdown` + `rehype-pretty-code`, auto ToC from headings (`rehype-slug` IDs).
- `src/lib/cache.ts`: Cache API keyed by path + `content_version` (`updated_at`); revalidates on publish/edit.
- `POST /api/analytics/track` (insert into `page_views`, fired client-side, no cookies).

---

# Phase 4 — Portfolio Section

**Exit criteria:** Portfolio browsable + tag-filterable publicly.

- `/portfolio` grid + tag filter.
- `/portfolio/[slug]`: description, tech stack, architecture md, screenshots (Supabase Storage URLs), github/demo links, lessons-learned md.
- Edge caching reused from Phase 3.

---

# Phase 5 — Home, Unified Tags, Dark Mode

**Exit criteria:** All four PRD sections live and navigable.

- Home: intro, featured blogs/projects (add `is_featured` bool via migration), social links.
- Shared tag-browsing UX across blogs/portfolio.
- Dark mode: CSS variables + `prefers-color-scheme` default, **cookie-persisted** (`src/lib/theme.ts`, read at edge to avoid flash), `ThemeToggle`.

---

# Phase 6 — Admin Completeness + Analytics

**Exit criteria:** Admin runs full content lifecycle without a DB console.

- Image upload: drag-drop in Tiptap → `/api/upload` → Supabase Storage → inline markdown `![](signedUrl)`.
- Tag management screen: rename, merge, delete.
- Per-blog analytics view (views over time, aggregated from `page_views`).

---

# Phase 7 — Hardening & Launch

**Exit criteria:** Production launch on custom domain.

- RLS audit (thoughts private; published-only visibility).
- Lighthouse pass, image optimization, cache-hit verification.
- CI deploy step (`wrangler deploy`), custom domain + SSL + redirects.
- Scheduled Supabase Postgres backup/export.

---

## Open items I'll confirm at the Phase 0 checkpoint
- Supabase project URL + service/anon keys + pooled `DATABASE_URL` (needed to run migrations & verify login/deploy).
- Cloudflare account + target Worker/domain (needed for `wrangler deploy` & CI secrets).
- Node package manager preference (default: **npm**; switch to pnpm if you prefer).

**No irreversible/outward-facing actions taken in Phase 0** — all local scaffolding + commits to the new repo inside `code/`. No deploy, no secrets created, no external services called.